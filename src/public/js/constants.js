import { KeyTypes } from "./types.js";

export const START_SOUND = "d";
export const STOP_SOUND = "u";

export const DEFAULT_FREQUENCY = 600;
export const MIN_FREQUENCY = 440;
export const MAX_FREQUENCY = 1600;

export const DEFAULT_SPEED = 100;
export const MIN_SPEED = 50;
export const MAX_SPEED = 200;

export const DEFAULT_KEY_TYPE = KeyTypes.Straight;

export const DEFAULT_MODE = "receive";
